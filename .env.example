# NextAuth server secret. Generate a new high-entropy value per environment.
NEXT_AUTH_SECRET=

# JWT signing secret used by the current NextAuth configuration.
JWT_SECRET=

# Canonical deployment origin used by NextAuth in production, for example:
# NEXTAUTH_URL=https://your-domain.example
NEXTAUTH_URL=
